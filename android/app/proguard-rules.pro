# Minification is off for the only build type that ships (debug, sideloaded),
# so this exists to keep the release build type resolvable rather than to tune
# anything. Add rules here if a signed release build is ever wired up.
